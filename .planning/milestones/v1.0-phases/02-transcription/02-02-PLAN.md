---
phase: 02-transcription
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/phonebot/pipeline/transcribe.py
  - docs/smart_format_analysis.md
autonomous: false
requirements: [STT-01, STT-02, STT-03]

user_setup:
  - service: deepgram
    why: "Deepgram Nova-3 API for German speech-to-text transcription"
    env_vars:
      - name: DEEPGRAM_API_KEY
        source: "Deepgram Console (console.deepgram.com) -> Settings -> API Keys -> Create Key"
    dashboard_config: []

must_haves:
  truths:
    - "data/transcripts/ contains exactly 30 JSON files (call_01.json through call_30.json)"
    - "Each JSON file contains the full Deepgram response with results.channels[0].alternatives[0].transcript"
    - "Re-running transcribe_all does NOT make any Deepgram API calls (all 30 cache files exist)"
    - "docs/smart_format_analysis.md documents smart_format behavior on German phone/email tokens across 5+ recordings with comparison tables"
    - "The analysis report contains a conclusion stating whether smart_format reliably converts German spoken-form tokens"
  artifacts:
    - path: "data/transcripts/call_01.json"
      provides: "Cached Deepgram JSON for recording 1"
      contains: "results"
    - path: "data/transcripts/call_30.json"
      provides: "Cached Deepgram JSON for recording 30"
      contains: "results"
    - path: "docs/smart_format_analysis.md"
      provides: "smart_format analysis report for Phase 3 prompt design"
      contains: "## Conclusion"
      min_lines: 30
  key_links:
    - from: "src/phonebot/pipeline/transcribe.py"
      to: "data/transcripts/*.json"
      via: "transcribe_all writes cache files"
      pattern: "data/transcripts"
    - from: "docs/smart_format_analysis.md"
      to: "data/transcripts/*.json"
      via: "analysis reads cached JSON to extract transcript text"
      pattern: "smart_format"
    - from: "docs/smart_format_analysis.md"
      to: "data/ground_truth.json"
      via: "expected phone/email values compared against transcript output"
      pattern: "ground_truth"
---

<objective>
Run live transcription on all 30 German recordings via Deepgram Nova-3, verify 30 cached JSON files, and generate the smart_format analysis report documenting spoken-form behavior.

Purpose: Produces the actual transcript data that Phase 3 needs for extraction, and documents the critical finding that smart_format does NOT format German phone numbers or emails (Phase 3 extraction prompts must handle spoken-form unconditionally).

Output: 30 cached JSON files in `data/transcripts/`, `docs/smart_format_analysis.md` analysis report, verification that cache skip works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcription/02-CONTEXT.md
@.planning/phases/02-transcription/02-RESEARCH.md
@.planning/phases/02-transcription/02-VALIDATION.md
@.planning/phases/02-transcription/02-01-SUMMARY.md

@src/phonebot/pipeline/transcribe.py
@data/ground_truth.json

<interfaces>
<!-- From Plan 01 — transcription module API -->

From src/phonebot/pipeline/transcribe.py (created in Plan 01):
```python
TRANSCRIPT_DIR = Path("data/transcripts")
CONCURRENCY = int(os.getenv("DEEPGRAM_CONCURRENCY", "5"))

async def _transcribe_one(client: AsyncDeepgramClient, wav_path: Path, semaphore: asyncio.Semaphore) -> tuple[str, Path]:
    """Transcribe single WAV; skip if cache present. Returns (recording_id, cache_path)."""

async def transcribe_all(recordings_dir: Path = Path("data/recordings")) -> dict[str, Path]:
    """Transcribe all WAV files. Returns dict of {recording_id: cache_path}."""

def get_transcript_text(cache_path: Path) -> str:
    """Extract plain transcript string from cached Deepgram JSON."""

def get_words(cache_path: Path) -> list[dict]:
    """Extract word-level data (timing, confidence, speaker) from cached JSON."""
```

From data/ground_truth.json (schema):
```json
{"recordings": [{"id": "call_01", "file": "call_01.wav", "expected": {"first_name": "...", "last_name": "...", "email": "...", "phone_number": "+49 ..."}}]}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run live transcription and generate smart_format analysis report</name>
  <files>src/phonebot/pipeline/transcribe.py, docs/smart_format_analysis.md</files>
  <read_first>
    - src/phonebot/pipeline/transcribe.py (the module created in Plan 01 — must understand its API to call it)
    - data/ground_truth.json (expected phone/email values to compare against transcript output in analysis)
    - .planning/phases/02-transcription/02-RESEARCH.md (smart_format German limitation details, report structure template, pitfalls 1-3)
  </read_first>
  <action>
    **Step 1: Verify DEEPGRAM_API_KEY is set**
    Run `uv run python -c "import os; from dotenv import load_dotenv; load_dotenv(); k=os.getenv('DEEPGRAM_API_KEY',''); print(f'Key set: {bool(k)} ({len(k)} chars)')"`.
    If the key is not set, STOP and create a checkpoint asking the user to set it in `.env`.

    **Step 2: Add a CLI runner to transcribe.py**
    Add a `__main__` block to `src/phonebot/pipeline/transcribe.py` so it can be invoked directly:
    ```python
    if __name__ == "__main__":
        import asyncio
        from rich.console import Console

        console = Console()
        console.print("[bold]Starting batch transcription...[/bold]")
        results = asyncio.run(transcribe_all())
        console.print(f"[green]Transcribed {len(results)} recordings.[/green]")
        for rid, path in sorted(results.items()):
            console.print(f"  {rid}: {path}")
    ```

    **Step 3: Run transcription**
    Execute: `uv run python -m phonebot.pipeline.transcribe`
    This will transcribe all 30 recordings and cache them to `data/transcripts/`.
    Expected output: 30 JSON files (call_01.json through call_30.json).
    Expected runtime: ~20-50 seconds with default concurrency of 5.

    **Step 4: Verify cache files**
    Run: `ls data/transcripts/ | wc -l` — must be 30.
    Run: `ls data/transcripts/` — should show call_01.json through call_30.json.
    Run: `uv run python -c "from phonebot.pipeline.transcribe import get_transcript_text; from pathlib import Path; print(get_transcript_text(Path('data/transcripts/call_01.json'))[:200])"` — should print first 200 chars of transcript.

    **Step 5: Verify cache skip (STT-03)**
    Run the transcription again: `uv run python -m phonebot.pipeline.transcribe`
    It should complete instantly (no API calls) and report 30 recordings. Time it to confirm it finishes in under 2 seconds.

    **Step 6: Generate smart_format analysis report**
    Create a Python script (inline or as a temp file) that:
    1. Loads `data/ground_truth.json` to get expected phone_number and email for each recording
    2. Loads cached JSON for at least 8 recordings (sample: call_01 through call_05, plus call_16, call_20, call_25 to cover foreign-name recordings)
    3. For each sampled recording:
       - Extracts the full transcript text via `get_transcript_text`
       - Extracts word-level data via `get_words` to inspect individual tokens
       - Compares expected phone number against what appears in the transcript (is it digit-formatted or spoken-form?)
       - Compares expected email against what appears in the transcript (is it assembled or spoken-form?)
       - Notes any foreign name transcription anomalies for calls 16+
    4. Writes `docs/smart_format_analysis.md` with this exact structure:

    ```markdown
    # Smart Format Analysis -- German Phone/Email Behavior

    ## Method
    - Nova-3, language="de", smart_format=True, punctuate=True, diarize=True, paragraphs=True
    - 30 recordings transcribed; {N} recordings sampled for detailed analysis
    - Expected values from data/ground_truth.json compared against raw transcript output

    ## Phone Number Observations

    | Recording | Expected (Ground Truth) | Transcript Contains | Formatted? | Notes |
    |-----------|------------------------|---------------------|------------|-------|
    | call_01   | {expected_phone}       | {what_transcript_shows} | {Yes/No} | {any notes} |
    | ...       | ...                    | ...                 | ...        | ... |

    ## Email Observations

    | Recording | Expected (Ground Truth) | Transcript Contains | Formatted? | Notes |
    |-----------|------------------------|---------------------|------------|-------|
    | call_01   | {expected_email}       | {what_transcript_shows} | {Yes/No} | {any notes} |
    | ...       | ...                    | ...                 | ...        | ... |

    ## Foreign Name Observations (Calls 16-30)

    | Recording | Expected Name | Transcribed As | Accurate? | Notes |
    |-----------|--------------|----------------|-----------|-------|
    | call_16   | {expected}   | {transcribed}  | {Yes/No}  | {notes} |
    | ...       | ...          | ...            | ...       | ... |

    ## Diarization Quality

    | Recording | Speaker Count | Bot/Caller Separation | Notes |
    |-----------|---------------|----------------------|-------|
    | call_01   | {N}           | {Clear/Unclear}       | {notes} |
    | ...       | ...           | ...                  | ... |

    ## Conclusion

    **smart_format German behavior:** {summary of findings}

    **Phone numbers:** {Are spoken-form digits converted to numerals? Expected: No}

    **Emails:** {Are spoken-form components assembled into addresses? Expected: No}

    **Diarization:** {Is speaker separation reliable?}

    **Foreign names:** {Are non-German names transcribed accurately?}

    **Implication for Phase 3:** {What must extraction prompts handle? Expected: LLM extraction prompts MUST handle spoken-form phone numbers and emails unconditionally. smart_format does NOT pre-normalize these for German.}
    ```

    The report generator should be a function added to `src/phonebot/pipeline/transcribe.py` called `generate_smart_format_report(sample_ids: list[str] | None = None) -> Path` that:
    - Defaults to sampling call_01 through call_05, call_16, call_20, call_25 (8 recordings)
    - Loads ground truth from `data/ground_truth.json`
    - For each sample, reads cached JSON and extracts transcript text + words
    - Searches the transcript for phone-like and email-like content
    - Writes the markdown report to `docs/smart_format_analysis.md`
    - Returns the report path

    Update the `__main__` block to also call `generate_smart_format_report()` after transcription.

    **Step 7: Run the report generator**
    Execute: `uv run python -m phonebot.pipeline.transcribe`
    Verify `docs/smart_format_analysis.md` exists and has the expected structure.

    **Step 8: Run full test suite**
    Execute: `uv run pytest -x` — all tests must pass (evaluation + transcription + CLI).
  </action>
  <verify>
    <automated>ls data/transcripts/*.json | wc -l && uv run python -c "from phonebot.pipeline.transcribe import get_transcript_text; from pathlib import Path; t=get_transcript_text(Path('data/transcripts/call_01.json')); print(f'Transcript length: {len(t)} chars')" && test -f docs/smart_format_analysis.md && echo "Report exists" && uv run pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls data/transcripts/*.json | wc -l` outputs 30
    - data/transcripts/call_01.json contains `"results"`
    - data/transcripts/call_30.json contains `"results"`
    - `uv run python -c "from phonebot.pipeline.transcribe import get_transcript_text; from pathlib import Path; print(get_transcript_text(Path('data/transcripts/call_01.json'))[:50])"` prints German text (not an error)
    - docs/smart_format_analysis.md exists
    - docs/smart_format_analysis.md contains `## Phone Number Observations`
    - docs/smart_format_analysis.md contains `## Email Observations`
    - docs/smart_format_analysis.md contains `## Conclusion`
    - docs/smart_format_analysis.md contains `Phase 3`
    - src/phonebot/pipeline/transcribe.py contains `def generate_smart_format_report(`
    - src/phonebot/pipeline/transcribe.py contains `if __name__`
    - `uv run pytest -x` exits 0
  </acceptance_criteria>
  <done>
    30 cached JSON files exist in data/transcripts/. Re-running transcription skips API calls (completes in under 2 seconds). smart_format analysis report exists at docs/smart_format_analysis.md with phone/email comparison tables across 8+ recordings and a conclusion section. Full test suite passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify smart_format analysis report and transcript quality</name>
  <files>docs/smart_format_analysis.md</files>
  <action>
    **What was built:** All 30 German recordings have been transcribed via Deepgram Nova-3 and cached as JSON. A smart_format analysis report has been generated comparing actual Deepgram output against expected phone numbers and emails from ground truth.

    **How to verify:**

    1. Open `docs/smart_format_analysis.md` and review:
       - Are the phone number comparison tables populated with real data from the transcripts?
       - Do the tables confirm that smart_format does NOT convert German spoken-form phone digits to numerals?
       - Do the email observations show that spoken-form email components are NOT assembled into addresses?
       - Does the conclusion section clearly state the implication for Phase 3?

    2. Spot-check transcript quality:
       - Run: `uv run python -c "from phonebot.pipeline.transcribe import get_transcript_text; from pathlib import Path; print(get_transcript_text(Path('data/transcripts/call_01.json')))"`
       - Does the transcript look like natural German dialogue?
       - Can you identify where the caller states their name, phone, and email?

    3. Verify cache skip:
       - Run: `time uv run python -m phonebot.pipeline.transcribe`
       - Should complete in under 2 seconds (no API calls)

    4. Check file count:
       - Run: `ls data/transcripts/ | wc -l`
       - Should output: 30

    **Resume signal:** Type "approved" if the analysis report accurately reflects transcript behavior and quality looks good, or describe any issues with the report or transcripts.
  </action>
  <verify>
    <automated>ls data/transcripts/*.json | wc -l && grep -c "## Conclusion" docs/smart_format_analysis.md && grep -c "Phase 3" docs/smart_format_analysis.md</automated>
  </verify>
  <done>
    Human has reviewed docs/smart_format_analysis.md and confirmed: phone/email comparison tables reflect actual Deepgram output, conclusion correctly identifies smart_format limitations for German, and transcript quality is sufficient for Phase 3 extraction.
  </done>
</task>

</tasks>

<verification>
- `ls data/transcripts/*.json | wc -l` outputs 30
- `uv run python -m phonebot.pipeline.transcribe` completes in under 2 seconds on second run (cache hit)
- `docs/smart_format_analysis.md` exists with phone/email comparison tables and conclusion
- `uv run pytest -x` passes (full test suite)
- Human has reviewed and approved the analysis report
</verification>

<success_criteria>
- 30 cached Deepgram JSON files exist (call_01.json through call_30.json)
- Each JSON file contains the full response structure (not just transcript text)
- Cache skip verified: second run makes zero API calls
- smart_format analysis documents phone/email behavior across 8+ recordings
- Analysis conclusion explicitly states implication for Phase 3 extraction prompts
- Human has verified report quality and transcript readability
</success_criteria>

<output>
After completion, create `.planning/phases/02-transcription/02-02-SUMMARY.md`
</output>
