---
phase: 04-observability
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [OBS-01, OBS-02]

must_haves:
  truths:
    - "Phoenix UI shows a 'phonebot-extraction' project with 30 independent traces after a full pipeline run"
    - "Each trace has distinct spans for the transcription node and the extraction node, with LLM latency visible"
    - "Traces are tagged with prompt version 'v1' — filterable in Phoenix UI"
    - "Re-running with --prompt-version v2 produces traces with a different version tag"
  artifacts: []
  key_links:
    - from: "run.py"
      to: "Phoenix UI at localhost:6006"
      via: "OTEL trace export during pipeline execution"
      pattern: "traces sent to Phoenix"
---

<objective>
Run the full extraction pipeline and verify Phoenix tracing works end-to-end: 30 traces appear in the Phoenix UI with correct span structure and metadata tagging.

Purpose: Validates that the observability integration from Plan 01 actually works with the live pipeline, real LLM calls, and the Phoenix UI. This is the phase gate — if traces appear correctly, Phase 4 success criteria are met.

Output: Verified Phoenix tracing with 30 traces, correct spans, and prompt version metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability/04-CONTEXT.md
@.planning/phases/04-observability/04-RESEARCH.md
@.planning/phases/04-observability/04-UI-SPEC.md
@.planning/phases/04-observability/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pipeline with tracing enabled</name>
  <files></files>
  <action>
    Run the full extraction pipeline to generate traces:
    ```bash
    uv run python run.py --prompt-version v1
    ```

    Verify console output matches UI-SPEC Interaction Flow Contract:
    1. Startup banner shows "Phonebot pipeline starting..."
    2. Config echo shows Model, Prompt version (v1), Recordings, Output
    3. "Tracing initialized -- project: phonebot-extraction" appears (per D-09, D-11)
    4. "Extracting from 30 recordings..." appears
    5. "Extraction complete in X.Xs" appears
    6. "Results written to outputs/results.json" appears
    7. Per-Field Accuracy Rich table appears
    8. "30 traces sent to Phoenix" appears (per D-14)
    9. "Phoenix UI: http://localhost:6006" appears (per D-03)

    If the output does not match the expected flow (e.g., Phoenix fails to start, traces not sent), diagnose and fix before proceeding to the checkpoint.

    IMPORTANT: If `px.launch_app()` returns None (RESEARCH Pitfall 4), check stderr for errors. Common fixes:
    - Port 6006 already in use: kill existing process or set PHOENIX_PORT env var
    - Missing dependency: re-run `uv add arize-phoenix arize-phoenix-otel openinference-instrumentation-langchain`

    If transcribe node span is NOT visible in traces (RESEARCH Open Question 2), add a manual span in `transcribe_node` in extract.py:
    ```python
    from opentelemetry import trace
    tracer = trace.get_tracer(__name__)
    async def transcribe_node(state: PipelineState) -> dict:
        with tracer.start_as_current_span("transcribe"):
            # ... existing code ...
    ```
    Then re-run the pipeline.
  </action>
  <verify>
    <automated>uv run python run.py --prompt-version v1 2>&1 | grep -c "traces sent to Phoenix"</automated>
  </verify>
  <done>Console output shows all 9 expected lines in correct order, including "30 traces sent to Phoenix" and Phoenix URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phoenix UI shows 30 traces with correct spans and metadata</name>
  <action>
    Human verifies Phoenix tracing end-to-end in the browser.

    What was built: Full Phoenix tracing integration — 30 pipeline runs with span-level visibility, metadata tagging, and persistent trace storage.

    How to verify:
    1. Open Phoenix UI at the URL printed by run.py (typically http://localhost:6006)
    2. Verify "phonebot-extraction" project exists in the project dropdown
    3. Verify 30 traces are listed (one per recording)
    4. Click on any trace (e.g., call_01) — verify it has child spans:
       - A "transcribe" span (showing cache load time)
       - An "extract" span (showing LLM call with latency)
    5. Check trace metadata: recording_id, model, prompt_version="v1", run_timestamp are visible
    6. Filter traces by prompt_version="v1" — all 30 should appear
    7. (Optional) Click into extract span — verify LLM input (transcript text) and output (CallerInfo JSON) are captured (per D-13)

    Resume signal: Type "approved" if Phoenix shows 30 traces with correct spans and metadata, or describe any issues.
  </action>
  <verify>Human visual inspection of Phoenix UI at localhost:6006</verify>
  <done>User confirms 30 traces visible with correct span structure (transcribe + extract), metadata tagging (recording_id, model, prompt_version), and LLM I/O captured</done>
</task>

</tasks>

<verification>
Phase 4 Success Criteria from ROADMAP.md:
1. Phoenix UI shows a "phonebot-extraction" project with 30 independent traces after a full pipeline run
2. Each trace has distinct spans for the transcription node and the extraction node, with LLM latency visible
3. Traces are tagged with a prompt version identifier — re-running with a different prompt produces traces with a different version tag
</verification>

<success_criteria>
- 30 traces visible in Phoenix UI under "phonebot-extraction" project
- Each trace has transcribe and extract child spans
- LLM latency visible in extract span
- Traces tagged with prompt_version="v1" and filterable
- Traces persist after run completes (Phoenix uses persistent SQLite storage)
- User approves Phoenix UI verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability/04-02-SUMMARY.md`
</output>
